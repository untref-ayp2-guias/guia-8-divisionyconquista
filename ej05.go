package guia8

// Sea `a` un vector ordenado de enteros todos distintos de tamaño `n`. Implementar un
// algoritmo de división y conquista, de complejidad _O(log n)_ en el peor caso, capaz
// de encontrar un índice `i` tal que `0≤i≤n-1` y `a[i] = i`, suponiendo que tal índice
// exista.
func ElementoEnSuPosicion(arreglo []int) int {
	return elementoEnSuPosicion(arreglo, 0, len(arreglo)-1)
}

func elementoEnSuPosicion(arreglo []int, inicio int, fin int) int {
	//implementar
	return 0
}
